package practice

fun main(){
    var fullName = "Kholikov Oybek"
    println(fullName)

    fullName = "Kholikov Sherbek"
    println(fullName)

    fullName = "" // blank line
    println(fullName)

    //Returns true if this char sequence is empty (contains no characters)
    println(fullName.isEmpty())

    var age = 30
    age = 32
    println(age)
}